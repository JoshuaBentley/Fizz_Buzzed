<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD"
      crossorigin="anonymous"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Geostar&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="style.css" />
    <title>Fizz Buzzed</title>
  </head>
  <body>
    <header class="container-fluid">
      <p class="title">Fizz Buzzed</p>
      <ul class="nav nav-tabs" id="myTab" role="tablist">
        <li class="nav-item" role="presentation">
          <button
            class="nav-link {{#if homeActive}}active{{/if}}"
            id="home-tab"
            data-bs-toggle="tab"
            data-bs-target="#home-tab-pane"
            type="button"
            role="tab"
            aria-controls="home-tab-pane"
            aria-selected="{{homeActive}}"
          >
            Home
          </button>
        </li>
        <li class="nav-item" role="presentation">
          <button
            class="nav-link {{#if menuActive}}active{{/if}}"
            data-bs-toggle="tab"
            data-bs-target="#menu-tab-pane"
            type="button"
            role="tab"
            aria-controls="menu-tab-pane"
            aria-selected="{{menuActive}}"
          >
            Menu
          </button>
        </li>
        <li class="nav-item" role="presentation">
          <button
            class="nav-link {{#if contactActive}}active{{/if}}"
            id="contact-tab"
            data-bs-toggle="tab"
            data-bs-target="#contact-tab-pane"
            type="button"
            role="tab"
            aria-controls="contact-tab-pane"
            aria-selected="{{contactActive}}"
          >
            Contact
          </button>
        </li>
        <li class="nav-item" role="presentation">
            <button
              class="nav-link {{#if loginActive}}active{{/if}}"
              id="login-tab"
              data-bs-toggle="tab"
              data-bs-target="#login-tab-pane"
              type="button"
              role="tab"
              aria-controls="login-tab-pane"
              aria-selected="{{loginActive}}"
            >
              Account
            </button>
          </li>
    </header>
    <div class="tab-content" id="myTabContent">
        <div class="tab-pane fade {{#if homeActive}}show active{{/if}}" id="home-tab-pane" role="tabpanel" aria-labelledby="home-tab" tabindex="0">
            <p>This is the home page</p>
        </div>
        <div class="tab-pane fade {{#if menuActive}}show active{{/if}}" id="menu-tab-pane" role="tabpanel" aria-labelledby="menu-tab" tabindex="0">